import { AtalhoEventoDirective } from './atalho-evento.directive';

describe('AtalhoEventoDirective', () => {
  it('should create an instance', () => {
    const directive = new AtalhoEventoDirective();
    expect(directive).toBeTruthy();
  });
});
